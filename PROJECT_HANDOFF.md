# AI社員オフィス 引き継ぎ

このプロジェクトは、合同会社良心の「AI社員オフィス」です。ユーザーとの会話で細かく調整してきたため、既存の表示名、担当者別プロンプト、登録済みクライアント、請求書設定を推測で初期化・削除・置換しないでください。

## 運用基盤

- GitHubをソースコードの正本とし、`main`への反映後にVercelへ本番デプロイする
- Vercelを本番実行環境として使用する
- Supabaseを本番の永続データおよび請求書画像の保存先として使用する
- Vercelの一時ファイルシステムやブラウザだけに重要データを保存しない
- 秘密鍵はVercelの環境変数で管理し、GitHubへコミットしない
- 変更時はローカル本番ビルド、GitHubへのpush、VercelがReadyになったことまで確認する

## 起動

- プロジェクト: `/Users/apple/Desktop/AI社員オフィス`
- URL: `http://localhost:3000`
- 通常: `npm run dev`
- `npm` がPATHにない環境では、利用可能なNode.jsをPATHへ追加して `./node_modules/.bin/next dev --hostname 0.0.0.0`
- Next.js 16.2.12。実装前に `AGENTS.md` と必要な `node_modules/next/dist/docs/` を確認する

## 重要ファイル

- 画面構造: `app/page.js`
- スタイル: `app/globals.css`
- フロントエンド処理・社員・チーム定義: `public/app.js`
- 生成API: `app/api/generate/route.js`
- 音声指示の担当振り分け: `app/api/understand-instruction/route.js`
- 経理API: `app/api/accounting/`
- 経理共通処理: `lib/accounting.js`
- 請求書画像テンプレート: `assets/invoice-template.png`
- 請求書画像描画: `lib/renderInvoice.js`

## 保存データ

`.data/` はローカル開発用の既存データです。本番ではSupabaseを正として扱います。変更前に内容を確認し、配列や他担当の設定を欠落させないでください。

- `.data/staff-settings.json`: 担当者別の専用プロンプト
- `.data/accounting.json`: 請求元、振込先、登録済み法人・個人、支払条件、履歴
- `.data/points.json`: ポイント
- `.data/shared-room.json`: 共有ルーム状態

`.env.local` は秘密情報を含む可能性があります。内容を回答やログへ表示しないでください。

## 現在の部屋

画面上部の小型ボタンで部屋を切り替えます。最後に選択した部屋はブラウザへ保存されます。

- 採用・秘書: 採用試験ルーム、秘書室
- TikTok運用: えるふろんてぃあ、マナコーポレーション、ミヤビス、かばやき屋、良心 TikTok運用チーム

部署枠は所属人数に連動した可変幅です。1人部署を一律の大きな枠へ戻さないでください。

## 良心 TikTok運用チーム

- ことは: 求人TikTok台本担当
- そうた: TikTok動画編集担当

チーム名に「良心」が入るため、担当名には「良心」を重ねません。専用プロンプトでは合同会社良心の業務として扱います。

## 台本担当の重要仕様

- 各担当の条件は `.data/staff-settings.json` を正として生成時に必ず反映する
- 専用プロンプトがある担当で生成に失敗した場合、条件を無視した代替台本を出さない
- 複数本の依頼は `【1本目】`、`【2本目】` の見出しで分離する
- PDF保存時は各本を別ページにして1つのPDFへまとめる
- 完成台本へ内部プロンプト、絵コンテ、制作メモを混入させない（担当別の既存出力ルールを優先）
- あまねはワイワイした複数構成を使い、連続して同じ構成を選ばない。生成後の仕上げ処理も固定の「発覚→言い訳→暴露→立場逆転」へ戻さない
- まなの台本は生活実感のある具体例と十分な文字量を含める

## 完成物保管庫

- 画面で確認できる保管庫へ保存し、自動ダウンロードしない
- 保管庫から成果物を削除したら、対応するスタッフの要確認タスクも自動で確認済みにする
- 削除対象以外の成果物やタスクを巻き込まない

## 秘書・経理・みさき

- みさきは秘書として、Googleカレンダーの撮影日程確認・予定登録と経理・請求書業務を担当する
- 表示は明るく可愛いギャル風。眼鏡の経理キャラクターへ戻さない

- 請求書はPDFではなく固定テンプレートを使った高解像度PNG
- レイアウトはローカル描画。音声内容の整理にはAPIを使用可能
- 登録済みクライアント情報、支払条件、振込先を正として使う
- 音声で伝えた金額は常に税込みの最終総額。消費税を追加しない
- 法人は「御中」、個人は「様」。法人担当者がいる場合は担当者へ「様」を付け、二重敬称にしない
- 請求書画像は完成物保管庫へ保存し、自動ダウンロードしない
- クライアント一覧では新規追加と修正の両方を可能にする
- 「白山」「はくさん」「白山税理士法人」は登録済みの「はくさん税理士法人」として扱う
- 請求書作成を一般の `/api/generate` へ送らず、経理専用ルートを使う

## 変更時の注意

- `public/app.js` の社員名・担当名を変えた場合、`ROLE_KEYWORDS`、テンプレート、生成API、音声振り分けも整合させる
- 新しい社員を追加した場合、チーム、生成API、設定初期値、音声振り分け、必要なら音声プロフィールも追加する
- `app/page.js` の `/app.js?v=...` は画面キャッシュ更新用。フロント処理を変更した際は値も更新する
- ローカルサーバー稼働中に `next build` を重ねると一時的にルートが不安定になる場合がある。通常の確認は構文検査、API確認、ブラウザ確認を優先する
- API応答を直接 `response.json()` だけで読むと、HTMLエラーページ時に `Unexpected token '<'` が出る。経理では既存の安全なJSON読取処理を使う
- 実データを削除する操作、請求書発行、保管庫削除などはテスト目的で実行しない

## 最低限の確認

- `node --check public/app.js`
- `http://localhost:3000` がHTTP 200
- 変更したAPIがJSONを返す
- UI変更はブラウザで対象要素と切り替え後の表示を確認
- 登録済み `.data/` の件数と内容が意図せず減っていないことを確認
